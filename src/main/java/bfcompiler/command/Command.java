package bfcompiler.command;

/**
 * Command interface for handling brainfuck commands.
 */
public interface Command {
    /**
     * Main method, which is executing command logic.
     */
    void execute();
}
